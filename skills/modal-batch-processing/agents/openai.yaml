interface:
  display_name: "Modal Batch Processing"
  short_description: "Choose Modal map, spawn, spawn_map, or batched workflows."
  default_prompt: "Use $modal-batch-processing to design or debug Modal fan-out, detached job, polling, concurrency, or dynamic batching workflows without drifting into serving, training, or sandbox questions."
  tags:
    - modal
    - python
    - batch
    - orchestration
