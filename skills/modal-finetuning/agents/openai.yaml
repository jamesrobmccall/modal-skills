interface:
  display_name: "Modal Fine-Tuning"
  short_description: "Design Modal GPU fine-tuning and post-training workflows."
  default_prompt: "Use $modal-finetuning to design or debug Modal training workflows for LLM SFT, LoRA, QLoRA, diffusion LoRA, YOLO, or GRPO without drifting into serving, orchestration, or sandbox lifecycle."
  tags:
    - modal
    - training
    - finetuning
    - gpu
