interface:
  display_name: "Modal LLM Serving"
  short_description: "Deploy and tune Modal-hosted vLLM or SGLang inference services."
  default_prompt: "Use $modal-llm-serving to deploy, benchmark, or debug a Modal-hosted vLLM or SGLang inference service."
  tags:
    - modal
    - llm
    - inference
    - gpu
