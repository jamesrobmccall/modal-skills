interface:
  display_name: "Modal Sandbox"
  short_description: "Create and operate Modal Sandboxes for code execution, tunnels, and persistence."
  default_prompt: "Use $modal-sandbox to create, attach to, debug, or persist a Modal Sandbox workflow."
  tags:
    - modal
    - sandbox
    - python
    - execution
